import React, {PureComponent} from 'react'
import { ElementTypes } from '../index'
import Bubble from '../Text/Bubble'
import styles from './index.css'


export default class QuickReplies extends PureComponent {

  getTextElement() {
    const { elements } = this.props
    const textElements = elements.filter((el) => el.type === ElementTypes.TEXT)
    return textElements[0] ? textElements[0] : null
  }

  getButtons() {
    const { elements } = this.props
    return elements.filter((el) => el.type === ElementTypes.BUTTON)
  }

  render() {
    const { elements, onClick } = this.props;
    const textElement = this.getTextElement()

    return (
      <div className={styles.QuickReplies}>
        {textElement && <Bubble text={textElement.text} />}
        {this.getButtons().map(({text, id, postback}) => {
          return <button key={id} onClick={() => onClick(postback, text)}>{text}</button>
        })}
      </div>
    )
  }

}
