import React from 'react'
import styles from './index.css'

export default function QuickReplies({ elements, onClick }) {
  return (
    <div className={styles.QuickReplies}>
      {elements.map(({text, id, postback}) => {
        return <button key={id} onClick={() => onClick(postback, text)}>{text}</button>
      })}
    </div>
  )
}
