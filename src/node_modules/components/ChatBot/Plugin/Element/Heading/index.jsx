import React from 'react'
import styles from './index.css'

export default function Heading ({ content, level = 1 }) {
  if (level === 1) {
    return <h1>{content}</h1>
  }
  return <h2>{content}</h2>
}
